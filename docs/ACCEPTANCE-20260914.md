# 阶段验收记录（2026-09-14）

## 双服务器临时联调

- 第一台：SSH 别名 18，Debian 12，Hub/Agent/systemd 备份。
- 第二台：SSH 别名 wsrser，Ubuntu 20.04.6，2 核、约 1.8 GiB 内存；普通用户 wsr。
- 范围：经用户批准，独立临时目录、低优先级 Agent、每 10 秒上报、最多 120 秒；不安装服务、不扫描目录、不修改现有服务或网络配置。使用仅监听回环的临时 SSH 转发，不占用原有 18080 端口。
- 测试 ID：wsrser-validation-20260913。Hub 同时显示两台在线，系统信息分别为 Debian 和 Ubuntu。
- 第二台收到 12 个样本，时间为 2026-09-13 22:47:29 至 22:49:19（香港时间）；日志显示 22:49:29 Agent 正常停止，后来 Hub 显示离线。
- 首段 28 秒测量 RSS 5688 KiB，CPU 累计时钟显示 0 秒（受计时精度限制，不等于零开销）。完整进程采样输出未保留，不能作为完整性能报告。
- 第二台主要原有监听端口仍在；Docker 和 site_total 为 active。容器或独立进程不能仅凭同名 systemd 单元判断健康，业务页面、数据库实际读写正确性需用户验证。
- 清理：确认 Agent 不再运行，撤销临时设备凭据，仅删除 agent、agent.token、agent.log 和空临时目录。临时转发进程已不存在。没有卸载软件、操作业务数据、重启服务或重启主机。
- 测试历史保留在第一台 Hub；离线记录和已撤销凭据用于追溯，不表示第二台仍安装监控。

## 独立恢复演练

从已生成的备份 seesize-20260913T190401.710547067Z.db 恢复到新路径，并在回环 18086 启动独立 Hub。验证管理员登录、2 条服务器记录、2 条设备记录、201 个趋势聚合点、事件查询接口和真实设备凭据认证均通过。上报只发往恢复实例，未修改实际 Hub。结束后停止恢复实例并删除临时恢复数据库。

## 磁盘完整流程

在第一台的独立临时 Hub（回环 18087）与专用文件目录执行：

1. 空目录建立基线。
2. 写入 2 MiB 测试文件；扫描识别 +2097152 字节，产生 1 条持久事件。
3. 登录会话标记事件已处理；处理时间有记录。
4. 删除本次创建的测试文件；扫描识别 -2097152 字节。
5. 以 1 个条目的预算再次扫描，结果不完整、不可比较、不生成新告警，原事件仍保留。

全部通过。只删除本次创建的文件和临时数据库，测试进程均已停止。

## 长期运行情况与未验收内容

第一台截至 2026-09-14 10:30（香港时间）累计 20278 个原始样本，最早记录为 2026-09-13 13:44，跨度约 20 小时 46 分钟。期间经历开发部署，不是严格的 24 小时无干预稳定性实验。Hub active、NRestarts=0；Agent active、NRestarts=1 对应此前主动模拟异常退出；最近备份结果 success，oneshot 执行完处于 inactive 属正常。

未完成：严格连续 24 小时的资源曲线分析、两台连续长期联调、大目录压力实验、真实网络中断后恢复、整机重启。第二台未执行这些可能影响业务的操作。

用户可配合确认：第二台现有业务页面可访问，核心读写操作正常。整机重启与实际网络中断只能在用户安排的维护窗口进行；当前不执行。
