# 2026-09-23 验收收尾

## 安全自动回归：通过

在本地临时 SQLite 数据库执行，未向业务服务发送错误登录或撤销请求。

- 会话有效期 8 小时：到期前 1 纳秒可读，到期即拒绝；重新登录不复活旧会话。
- 注册码 10 分钟到期拒绝兑换，未创建设备。
- 错误凭据拒绝且不签发 Cookie；一分钟内第 21 次登录限流，窗口结束后恢复。
- 原有回归：未登录拒绝读取、写操作保护、注册码不重复兑换、跨设备令牌拒绝、撤销后旧令牌及共享令牌均不可冒用、退出立即失效。
- Secure/HttpOnly/SameSite Cookie 策略及不信任转发头测试通过。非真实公网 HTTPS 验收。
- 全部 Go 测试及 go vet 通过。新增测试位于 internal/hub/security_acceptance_test.go。

登录限流当前为全局窗口，错误请求可能暂时阻止合法用户登录；本次验证现有行为，不等于完整安全审计或抗拒绝服务能力证明。

## 新机只读检查：未发现阻塞问题

- Hub 和 Agent active，NRestarts 均为 0。Hub 最近启动为 9 月 22 日 23:33:05（图表升级），Agent 自 9 月 17 日 21:30:32 运行。计数为当前 systemd 状态，不代替完整历史审计。
- 最近 24 小时共 8640 个指标样本，最新样本距检查约 6.07 秒；相邻采样最大间隔 10.053 秒，无超过 35 秒的间隔。采集时间连续不单独证明网络接收延迟始终正常。
- 当前 RSS：Hub 18728 KiB，Agent 6656 KiB；进程生命周期高水位分别 18728/10632 KiB。这是单次读取，不是内存长期趋势或泄漏排除报告。
- 最近 24 小时服务日志未检出 WARN/ERROR/failed；journald 优先级检查亦无警告记录。
- 9 月 23 日 03:02:01 备份任务成功退出，状态 0；当前保留 7 份，最新备份 quick_check=ok，包含 45189 个指标。未触发额外备份或重启。
- Docker、site_total active；用户确认原有业务页面和操作正常。

## 用户提供的人工和虚拟机结果

- 正常窗口退出后接口拒绝访问：通过。
- 恢复实例登录和历史访问：通过；备份/恢复库此前均为 43916 条指标。非空快照和事件不在本次恢复样本内。
- Ubuntu x86_64，2 核 2 GiB 虚拟机，修正脚本后 1000/5000/10000 文件均 PASS。

| 文件数 | 秒 | 扫描峰值 RSS KiB | CPU 秒 | 新增心跳 |
| --- | --- | --- | --- | --- |
| 1000 | 5.009 | 30144 | 0.122515 | 5 |
| 5000 | 25.010 | 30188 | 0.689366 | 25 |
| 10000 | 50.012 | 30144 | 1.283353 | 50 |

三轮逻辑大小正确、完整扫描、限额不完整标记及代理断连恢复通过。证据为用户粘贴的报告内容，原始 JSON 尚未归档。固定 200 条目/秒速率，不代表最大性能；独立扫描子进程并非 Agent 内置扫描，也未测严格心跳最大延迟。

## 结论与剩余边界

当前基础验收未发现阻止后续开发的问题。仍保留：真实 HTTPS、维护窗口内整机重启、双 Agent 持续隔离验证、Agent 内置定时扫描长期观察。网络多网卡合计含虚拟接口的统计口径需后续明确。只读检查不构成新机 24 小时资源挂测。
