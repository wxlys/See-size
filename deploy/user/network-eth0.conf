# wsrser-specific override; verify interface name before using elsewhere.
[Service]
ExecStart=
ExecStart=%h/seesize/seesize-agent-linux-amd64 -hub http://127.0.0.1:18081 -id wsrser-main -token-file %h/seesize/agent.token -interval 10s -network-interfaces eth0
